# 前端请求 API 的前缀。留空 = 同源（nginx 镜像 / werewolf-server 直接托管时都用这个）。
# 静态 HTML 部署到别的域名时，填编排端的完整地址，例如 https://werewolf-api.example.com
VITE_API_BASE=

# 仅 `npm run dev` / `npm run preview`：/api 代理到哪里
WEREWOLF_API_PROXY=http://127.0.0.1:8000
